#!/bin/sh
# Block commits containing secrets. Enable with: git config core.hooksPath .githooks
if ! command -v gitleaks >/dev/null 2>&1; then
    echo "pre-commit: gitleaks not installed (brew install gitleaks) — refusing to commit unscanned." >&2
    exit 1
fi
exec gitleaks protect --staged --verbose
